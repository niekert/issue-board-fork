import styled from 'styled-components';
import {FaPen} from 'react-icons/fa';

const EditTrigger = styled(FaPen)`
    margin-left: 10px;
    font-size: 10px;
    cursor: pointer;

    &:hover {
        color: #0b71f1;
    }
`;

export default EditTrigger;
